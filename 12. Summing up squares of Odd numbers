#include <stdio.h>
int main() 
{
    int n,i,sum=0;
    
    printf("Enter the odd number: ");
    scanf("%d",&n);
    
    for (i=0;i<=n;i++)
	{
        if(i%2!=0)
        sum+=i*i;
    }
    printf("The sum of squares of odd numbers upto %d:  %d",n,sum);
    
    return 0;
}
