#include<stdio.h>
int main()
{
	int i;
	printf("enter any number: ");
	scanf("%d",&i);
	if(i==0)
	{
		printf("enter a valid number");
	}
	else if(i%2==0)
	{
		printf("even number");
	}
	else
	{
		printf("odd number");
	}
}
